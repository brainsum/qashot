version: "2"

services:
  mariadb:
    restart: always

  php:
    environment:
      PROJECT_ENVIRONMENT: 'production'
    restart: always

  nginx:
    restart: always

  mailhog:
    restart: always
