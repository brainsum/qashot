version: "2"

services:

  mariadb:
    restart: always

  php:
    restart: always
    environment:
      PHP_SENDMAIL_PATH: /usr/sbin/sendmail -t -i

  nginx:
    restart: always
  
  pma:
    image: tianon/true
    restart: always

  mailhog:
    restart: always

#  backstop_node:
#    restart: always

  portainer:
    restart: always

  traefik:
    ports:
      - '80:80'
    restart: always
